import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Separator = ({spacing}) => {
  return <View style={{width: '100%', padding: spacing}}></View>;
};

export default Separator;
